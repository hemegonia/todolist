<% include partials/header %>

<a class="ui button green huge fa-beat" href="/demo">
   Try the demo!
   <h5>(no login required)</h5>
</a>
<div class="row"></div>
<h4 class="ui divider horizontal">OR</h4>

<div class="row">
   <form class="ui large form" action="/login" method="POST">
      <div class="ui segment">
         <div class="field">
            <div class="ui left icon input">
               <i class="user icon"></i>
               <input type="text" name="username" placeholder="Username" />
            </div>
         </div>
         <div class="field">
            <div class="ui left icon input">
               <i class="lock icon"></i>
               <input type="password" name="password" placeholder="Password" />
            </div>
         </div>
         <button class="ui submit fluid button primary">Login</button>
      </div>
   </form>
</div>
<h4 class="ui divider horizontal">OR</h4>
<div class="row">
   <form class="ui large form" action="/register" method="POST">
      <div class="ui segment">
         <div class="field">
            <div class="ui left icon input">
               <i class="user icon"></i>
               <input type="text" name="username" placeholder="Username" />
            </div>
         </div>
         <div class="field">
            <div class="ui left icon input">
               <i class="lock icon"></i>
               <input type="password" name="password" placeholder="Password" />
            </div>
         </div>
         <button class="ui submit fluid button secondary">Sign Up</button>
      </div>
   </form>
</div>

<% include partials/footer %>
